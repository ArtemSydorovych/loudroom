// TODO: implement — route structure:
// "/" — redirect based on auth state
// "/dashboard" — presenter dashboard (protected)
// "/sessions/:id" — live session view (protected)
// "/join" — public join page
// "/join/:code" — public join with pre-filled code
// "/audience/sessions/:id" — public audience session view
